"""Cortex — a local semantic layer over an Obsidian vault, exposed via MCP.

Smart Connections lives inside Obsidian. Cortex puts the same embedding-based
recall behind an MCP server, so every Claude surface (Code, desktop, web) and
any future agent can query one brain.
"""

__version__ = "0.1.0"
